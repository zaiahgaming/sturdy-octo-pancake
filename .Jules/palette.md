## 2026-07-10 - UX and Accessibility Improvements
**Learning:** Adding ARIA attributes like `aria-live` and `aria-label` improves screen reader compatibility. Setting the cursor to `crosshair` visually indicates aiming behavior. Focusing buttons dynamically ensures keyboard navigation is seamless during UI state changes.
**Action:** Applied ARIA attributes to HTML elements, updated canvas cursor in CSS, and called `.focus()` on the Restart button in JavaScript.